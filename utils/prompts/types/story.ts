export const storyPrompt = (topic: string) => `
Create a story in Spanish ${topic ? `about ${topic}` : ''}.

Style guidelines:
- Include engaging characters and dialogue
- Use narrative elements (setting, conflict, resolution)
- Focus on everyday vocabulary in context
- Include descriptive language appropriate to the story length
- Make it culturally relevant to Spanish-speaking countries when possible
` 